import { Render } from '@/AxzoEzForm/core/componentFactory'

const quantityPriceFormRender:Render = (props, ctx) => {
  return (
        <div>this is pc  form render</div>
  )
}
export default quantityPriceFormRender
